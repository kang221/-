#### CMake

Cross-platform Make是一个跨平台的构建系统生成工具。既不是编译器，也不是编程语言，它是一个“告诉编译器和IDE怎么编译项目”的工具。可以用于在不同的系统上生成合适的构建文件。

通俗的来讲，代码的运行是往往可能是在不同的系统里面进行，而每个系统的库的路径可能都不一样，因此cmake的作用就是，自动在系统默认路径等地搜集我们需要用的库的位置（我们需要用的库，是自己下载的，cmake不会帮我们下载库）。